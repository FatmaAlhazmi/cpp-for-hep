# Lesson 11: Debugging

In this lesson, we practice reading compiler errors.

`main.cpp` is correct.  
`broken.cpp` is intentionally broken.  
`task_solution.cpp` is the fixed version.

## Files

- `main.cpp`
- `broken.cpp`
- `task_solution.cpp`
