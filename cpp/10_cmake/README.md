# Lesson 10: CMake

In this lesson, we build a small C++ project using CMake.

CMake uses `CMakeLists.txt` to describe how to build the executable.

## Files

- `main.cpp`
- `four_vector.hpp`
- `four_vector.cpp`
- `CMakeLists.txt`
- task solution files
